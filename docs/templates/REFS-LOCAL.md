# REFS-LOCAL.md — Local Reference Paths

> This file is gitignored. Record hard-coded local paths to reference files, repos,
> or resources on this machine that are needed to operate this repository.
> Do not commit machine-specific absolute paths.

## Portfolio Root

<!-- Uncomment and set when working in the portfolio workspace:
- Portfolio root: /path/to/portfolio/root
-->

## Local Repo Paths

<!-- Paths to sibling or dependency repos on this machine:
- auto-pass: /path/to/util-repos/auto-pass
- shock-relay: /path/to/util-repos/shock-relay
-->

## Local File Paths

<!-- Paths to local config, data, or secret files that this repo reads at runtime:
- Config override: /path/to/local/config.toml
- Data directory: /path/to/local/data/
- Secrets file: /path/to/local/.env
-->

## Notes

<!-- Any context or usage notes about these local paths -->
